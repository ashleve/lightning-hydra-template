# @package _group_

# Weights&Biases logger config
wandb:
    class: pytorch_lightning.logger.WandbLogger
    args:
        project: "env_tests"
        entity: "lodz"  # set to name of your wandb team or just remove
        offline: False  # set True to store all logs only locally
        job_type: "train"
        save_dir: "."
